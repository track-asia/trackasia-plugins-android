<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="org.trackasia.android.plugins.offline">
    <application>
        <!-- Offline download region picker activity -->
        <activity
            android:name="org.trackasia.android.plugins.offline.ui.OfflineActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />

        <!-- Offline download service -->
        <service
            android:name="org.trackasia.android.plugins.offline.offline.OfflineDownloadService"
            android:exported="false" />
    </application>
</manifest>